# Data Staging

This directory is prepared for dataset staging. By default, only placeholder instructions are provided to respect dataset licenses. Use the scripts in `src/data_collection` to prepare folders and notes.
